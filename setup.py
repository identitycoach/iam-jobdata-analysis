from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Analysis of Identity & Access Management job listings in python',
    author='Identity Coach',
    license='',
)
